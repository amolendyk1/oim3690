# Week 02 Log

Week: 5/25 - 05/27

> **What this is**: a short weekly summary of what you built, learned, and how you used AI. Not a diary — just structured evidence of your work.
>
> **Format**: use proper Markdown — headings, bullet points, code blocks. This file will be rendered on GitHub. If it looks like plain text with no formatting, you're doing it wrong.
>
> **Required**: all 4 sections present and non-empty. Empty sections = incomplete log.

## What I worked on

- In class today, we worked on `hello.html`. We wrote version 1 and edited it to create a more descriptive version 2.
- We learned about different HTML tags such as `h1`, `p`, and `ul`.
- I had questions about what these tags meant in the previous class, so I enjoyed learning about the different HTML tags.
- We also learned about the different components in the `<head>` section, such as `<title>My Page</title>`, `<meta charset="UTF-8">`, and `<meta name="viewport" ...>`.
- For homework, we created `about-me.html`. This included using AI to create a page with hierarchy, `header`, `main`, `footer`, a local image, and a URL.

## What I learned

- From this class and the exercises, I learned that it is important to know what each of the HTML tags means.
- The tags help a lot with formatting your HTML page, so being able to use them to organize your code is helpful for readability.
- Also, by using AI for `hello.html`, I learned the differences between CSS and HTML.

## AI interactions

### Interaction 1

- **What I asked**: In class, I asked AI to take `hello.html` and add a navigation bar, a section with my interests, and a footer.
- **What AI gave me**: It gave me a fully formatted page with different sections including “About”, “Interests”, and a footer.
- **What I kept / changed / rejected**: It gave me a CSS version instead of just using HTML tags. I then had to ask it to format using HTML instead.

### Interaction 2

- **What I asked**: I asked AI to create an `about-me.html` using `header`, `main`, `footer`, proper heading hierarchy, one local image, and one descriptive URL with descriptive alt text. In addition, at least one link, one list, and one paragraph.
- **What AI gave me**: It gave me a fully formatted page including a bio, interest section, image with a description, and a footer with contact information.
- **What I kept / changed / rejected**: I kept mostly everything but the headings were not formatted correctly, so I had to change that. I also changed the picture and put in my own picture since it created one for me. I changed the image code because the image was not opening. I changed the name of the links to better describe what the pages were about. Lastly, I had to adjust the code it gave me because it only gave me a local image and not the external image too.

## Questions

- A question I have is: is there a preference between using HTML tags vs CSS, or is it solely just formatting differences?
- Does CSS also have `<head>`, `<main>`, and `<footer>`?



# Week 05 Log

Week: 06/15 - 06/17

> **What this is**: A short weekly summary of what you built, learned, and how you used AI. Not a diary — just structured evidence of your work.
>
> **Format**: Use proper Markdown — headings, bullet points, code blocks. This file will be rendered on GitHub. If it looks like plain text with no formatting, you're doing it wrong.
>
> **Required**: All 4 sections present and non-empty. Empty sections = incomplete log.

## What I worked on

- In class 6/22, we worked on review questions about Dom functions sucha as `queryselector` and `queryElementById`. We also created a todo app using `index.html`. This was a simple html file with css and JavaScript with not special formatting yet. We then went through our code and looked for specific functions such as where `<script>` was, `queryselector`, `getelementbyid`, `addeventlistener`, `let`, `const` and `function`. Then, we sectioned the Javascript out into a seperate file `script.js` and moved everything from `<script>` into that file and we linked it into `index.html`. We then compared our files in groups to see similarities and differences. 
- In class 6/24, we worked on javascript practice problems. For example, variables such as `const` vs `let. We also worked on our todo app and compared it to others in the class. We also started working on a `converter.html`. 

## What I learned

- I learned that `const` cannot reassign the value but using `let` will let you reassign the value. 
- This week I learned how to connect html elements to Javasript by creating my app. I learned how to use `querySelector`, `getElementById`, and `addEventListener`. 

## AI interactions

### Interaction 1

- **What I asked**: I asked ai to help create a todo app and write the style. 
- **What AI gave me**: Ai gave me an app that I did not really like so I had to give it inspriation pictures to accurately create a functioning todo app.
- **What I kept / changed / rejected**: I changed all of the formatting but I kept the actual functions and how the code ran. 

### Interaction 2

- **What I asked**: I asked AI to read my proposal and ask my questions to create `prd.md`.  
- **What AI gave me**: Ai asked me questions and wrote a detailed plan for how to go about altering my project.
- **What I kept / changed / rejected**: I just had to alter the formatting to make it markdown formatting. 

## Questions

-  A question I have is how to add a drag and drop feature?
-  Another question I have is how to caculate daily nutrition?

## Todo Inclass Comparison 
- One thing that someone had was a due date for the different tasks.
- One thing different that I had were filters and tags to section the different tasks.

## MP2 Findings
- I chose a semantic layout to easily visualize all of the elements and pages. My first piece of javascript as a navagation system to switch from different pages. From the AI generated parts, I had to understand how to use render to build hte grid and fill in each slot. 
- The hardest interaction to implement was assigning meals to the correct slot. Some javascript I used included `modalday`, `renderPlanner`, `queryselector`, etc. Some bugs I found included undefined planner slot. The planner had slots but the meals were not being accurately sorted. 
- Some feedback I recieved was combining the pantry and meal tab. Some edge cases I handle was an empy meal library, I had to work on it saying that there are no meals instead of the app just breaking. If I had more time I would have daily nutrition breakdown instead of weekly. 